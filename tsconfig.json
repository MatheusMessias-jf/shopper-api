{
  "compilerOptions": {
    "target": "ES2020",                                  /* Define a versão do JavaScript emitido */
    "module": "ESNext",                                  /* Define o tipo de módulo a ser usado (compatível com import/export) */
    "moduleDetection": "force",                          /* Controla como os módulos são detectados */
    "rootDir": "./src",                                  /* Especifica a pasta raiz dos arquivos de origem */
    "outDir": "./dist",                                  /* Especifica a pasta de saída para os arquivos compilados */
    "baseUrl": ".",                                      /* Define o diretório base para a resolução de módulos */
    "paths": {
      "@/": ["./src/"]                                   /* Mapeia "@/..." para "./src/..." */
    },                                      
    "moduleResolution": "node",                          /* Usa a resolução de módulos do Node.js */
    "esModuleInterop": true,                             /* Permite a interoperabilidade entre módulos CommonJS e ES6 */
    "forceConsistentCasingInFileNames": true,            /* Força consistência de capitalização nos nomes de arquivos */
    "strict": true,                                      /* Habilita todas as verificações de tipo estritas */
    "skipLibCheck": true,                                /* Pula a verificação de tipos em arquivos de definição (.d.ts) */
    "resolveJsonModule": true                            /* Permite a importação de arquivos JSON como módulos */
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]                            /* Exclui a pasta node_modules */
}